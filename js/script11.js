let faltas = parseInt(prompt("Digite o número de faltas do aluno:"));

if (faltas > 15) {
    alert("O aluno foi reprovado por faltas.");
} else {
    alert("O aluno está dentro do limite de faltas.");
}